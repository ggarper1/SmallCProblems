name: Ensure Conventional PR Title
on:
  pull_request_target:
    types: [ready_for_review]
    branches: [develop, main]
permissions:
  pull-requests: write

jobs:
  enforce:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v7
        with:
          script: |
            const pr = context.payload.pull_request;
            let title = pr.title || "";
            const labels = (pr.labels || []).map(l => (l.name || "").toLowerCase());

            // already conventional
            if (/^(feat|fix|docs|refactor)(!?)\s*:/i.test(title)) {
              core.info("Title already conventional");
              return;
            }

            // Map -> prefix + emoji
            const has = (n) => labels.includes(n);
            let prefix = null, emoji = "";
            if (has("feature") || has("feat")) { prefix = "feat"; emoji = " ✨"; }
            else if (has("bug"))               { prefix = "fix";  emoji = " 🐛"; }
            else if (has("refactor"))          { prefix = "refactor"; emoji = " ♻️"; }

            if (!prefix) {
              core.info("No matching labels -> skipping");
              return;
            }

            // Add "issue N" if feature/issue-12-...
            const ref = pr.head && pr.head.ref || "";
            const m = false; // ref.match(/issue[-/](\d+)/i);
            const issueStr = m ? ` issue ${m[1]}` : "";

            const newTitle = `${prefix}:${emoji}${issueStr} ${title}`.replace(/\s+/g, " ").trim();

            await github.rest.pulls.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: pr.number,
              title: newTitle
            });
            core.info(`Updated PR title -> ${newTitle}`);
